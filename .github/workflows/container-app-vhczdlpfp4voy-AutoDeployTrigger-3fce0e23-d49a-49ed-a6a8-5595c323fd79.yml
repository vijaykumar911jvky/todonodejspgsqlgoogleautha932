name: Trigger auto deployment for container-app-vhczdlpfp4voy

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-vhczdlpfp4voy-AutoDeployTrigger-3fce0e23-d49a-49ed-a6a8-5595c323fd79.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPVHCZDLPFP4VOY_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPVHCZDLPFP4VOY_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPVHCZDLPFP4VOY_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPVHCZDLPFP4VOY_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPVHCZDLPFP4VOY_REGISTRY_PASSWORD }}
          containerAppName: container-app-vhczdlpfp4voy
          resourceGroup: todonodejspgsqlgoogleautha93284f0
          imageToBuild: default/container-app-vhczdlpfp4voy:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


